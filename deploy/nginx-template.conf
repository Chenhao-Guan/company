# Nginx 配置模板 - 厦门联信备件有限公司网站
#
# 此文件仅作参考，实际使用时 deploy.sh 会自动生成配置

# HTTP 服务器配置
server {
    listen 80;
    server_name xiamenunion.com www.xiamenunion.com;

    # 访问日志
    access_log /var/log/nginx/xiamen-union-access.log;
    error_log /var/log/nginx/xiamen-union-error.log;

    # 反向代理到 Next.js 应用
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 静态资源缓存优化
    location /_next/static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 200 60m;
        add_header Cache-Control "public, immutable";
    }

    location /image {
        proxy_pass http://localhost:3000;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /video {
        proxy_pass http://localhost:3000;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS 服务器配置（SSL 证书安装后自动添加）
# server {
#     listen 443 ssl http2;
#     server_name xiamenunion.com www.xiamenunion.com;
#
#     # SSL 证书路径
#     ssl_certificate /etc/letsencrypt/live/xiamenunion.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/xiamenunion.com/privkey.pem;
#
#     # SSL 配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # 日志
#     access_log /var/log/nginx/xiamen-union-ssl-access.log;
#     error_log /var/log/nginx/xiamen-union-ssl-error.log;
#
#     # 反向代理配置同上
#     location / {
#         proxy_pass http://localhost:3000;
#         # ... 其他配置同上
#     }
# }
#
# # HTTP 重定向到 HTTPS
# server {
#     listen 80;
#     server_name xiamenunion.com www.xiamenunion.com;
#     return 301 https://$server_name$request_uri;
# }
